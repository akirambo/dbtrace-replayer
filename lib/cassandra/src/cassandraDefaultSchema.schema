
CREATE TABLE testdb.string (key TEXT, value TEXT, primary key (key)) ;

CREATE TABLE testdb.array (key TEXT, value SET<TEXT>, primary key (key)); 

CREATE TABLE testdb.list (key TEXT, value LIST<TEXT>, primary key (key)); 

CREATE TABLE testdb.sarray (key TEXT, value MAP<TEXT,FLOAT>, primary key (key)); 

CREATE TABLE testdb.hash (key TEXT, value MAP<TEXT,TEXT>, primary key (key)); 